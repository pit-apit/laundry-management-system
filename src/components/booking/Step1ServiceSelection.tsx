'use client';\n\nimport { useState } from 'react';\nimport Link from 'next/link';\n\nconst SERVICES = [\n  { id: '1', name: 'Cuci Komplit', description: 'Layanan cuci lengkap dengan pencucian dan pengeringan', icon: '🧼' },\n  { id: '2', name: 'Cuci Kering', description: 'Layanan cuci kering tanpa air', icon: '🫧' },\n  { id: '3', name: 'Setrika Saja', description: 'Layanan setrika tanpa pencucian', icon: '👕' },\n];\n\nexport default function Step1ServiceSelection({\n  selectedService,\n  onNext,\n}: {\n  selectedService?: string;\n  onNext: (serviceId: string) => void;\n}) {\n  const [selected, setSelected] = useState(selectedService);\n\n  const handleNext = () => {\n    if (!selected) {\n      alert('Pilih layanan terlebih dahulu');\n      return;\n    }\n    onNext(selected);\n  };\n\n  return (\n    <div className=\"min-h-screen bg-gradient-to-br from-primary-50 to-primary-100 p-4\">\n      <div className=\"max-w-2xl mx-auto\">\n        {/* Header */}\n        <div className=\"mb-8\">\n          <Link href=\"/\" className=\"text-primary-600 hover:text-primary-700 mb-4 inline-block\">\n            ← Kembali\n          </Link>\n          <h1 className=\"text-4xl font-bold text-primary-900\">Pilih Layanan</h1>\n          <p className=\"text-primary-700 mt-2\">Langkah 1 dari 8 - Pilih jenis layanan laundri</p>\n        </div>\n\n        {/* Progress Bar */}\n        <div className=\"mb-8\">\n          <div className=\"flex justify-between mb-2\">\n            <span className=\"text-sm font-semibold text-primary-600\">Progres: 1/8</span>\n          </div>\n          <div className=\"w-full bg-primary-200 rounded-full h-2\">\n            <div className=\"bg-primary-600 h-2 rounded-full\" style={{ width: '12.5%' }}></div>\n          </div>\n        </div>\n\n        {/* Service Cards */}\n        <div className=\"space-y-4 mb-8\">\n          {SERVICES.map((service) => (\n            <div\n              key={service.id}\n              onClick={() => setSelected(service.id)}\n              className={`p-6 rounded-lg cursor-pointer transition-all border-2 ${\n                selected === service.id\n                  ? 'border-primary-600 bg-primary-50 shadow-lg'\n                  : 'border-primary-200 bg-white hover:border-primary-300'\n              }`}\n            >\n              <div className=\"flex items-start gap-4\">\n                <div className=\"text-4xl\">{service.icon}</div>\n                <div className=\"flex-1\">\n                  <h3 className=\"text-xl font-bold text-primary-900\">{service.name}</h3>\n                  <p className=\"text-primary-700 mt-1\">{service.description}</p>\n                </div>\n                <div\n                  className={`w-6 h-6 rounded-full border-2 transition-all ${\n                    selected === service.id\n                      ? 'border-primary-600 bg-primary-600'\n                      : 'border-primary-300'\n                  }`}\n                >\n                  {selected === service.id && (\n                    <div className=\"w-full h-full flex items-center justify-center\">\n                      <div className=\"w-2 h-2 bg-white rounded-full\"></div>\n                    </div>\n                  )}\n                </div>\n              </div>\n            </div>\n          ))}\n        </div>\n\n        {/* Navigation Buttons */}\n        <div className=\"flex justify-end gap-4\">\n          <Link\n            href=\"/\"\n            className=\"px-6 py-3 border-2 border-primary-600 text-primary-600 rounded-lg hover:bg-primary-50 transition font-semibold\"\n          >\n            Batal\n          </Link>\n          <button\n            onClick={handleNext}\n            disabled={!selected}\n            className=\"px-6 py-3 bg-primary-600 text-white rounded-lg hover:bg-primary-700 disabled:opacity-50 transition font-semibold\"\n          >\n            Lanjut →\n          </button>\n        </div>\n      </div>\n    </div>\n  );\n}\n